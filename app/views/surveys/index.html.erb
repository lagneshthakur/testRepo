<div class="">
	<div class="header">
	  <h1>Surveys</h1>
	</div>
	  <% @surveys.each do |survey| %>
	    <div class="surveys">
	      <p class="link"><%= link_to survey.title, survey_path(:id => survey.id) %></p>
	  <% end %>
	   	<!-- <p class="link"><%#= link_to 'New Survey', new_survey_path %></p> -->
		</div>
</div>
